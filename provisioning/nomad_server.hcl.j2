server {
  enabled = true
  bootstrap_expect = 1
}

# If you want your server to be client as well, enable it below
client {
  enabled = true
  network_interface = "eth1"
}

plugin "raw_exec" {
  config {
    enabled = true
  }
}